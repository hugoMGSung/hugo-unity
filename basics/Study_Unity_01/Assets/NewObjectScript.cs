using UnityEngine;

public class NewObjectScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("첫 유니티 스크립트!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
